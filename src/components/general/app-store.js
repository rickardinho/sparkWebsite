import React from 'react';
import AppStoreIcon from './../../images/icons/download-on-the-app-store.svg';

const AppStore = () => {

    return (
        <a href=''><img alt='Get it on the App Store' src={AppStoreIcon} height="50" /></a>
    );
};

export default AppStore;
